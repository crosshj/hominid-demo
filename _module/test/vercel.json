{
    "buildCommand": "npm run build",
    "devCommand": "npm run start",
    "rewrites": [
        {
            "source": "/api/auth/(.*)",
            "destination": "/api/auth"
        },
        {
            "source": "/api/(.*)",
            "destination": "/api"
        }
    ]
}
